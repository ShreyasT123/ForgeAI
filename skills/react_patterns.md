# React Patterns Skill
Expert in modern React (v18+) and TypeScript patterns.

## Guidelines
- Use Functional Components and Hooks.
- Prefer `interface` over `type` for component props.
- Implement error boundaries for robust components.
