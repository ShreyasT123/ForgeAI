# SQL Expert Skill
Expert in writing optimized PostgreSQL and SQLite queries.

## Guidelines
- Use CTEs for complex logic.
- Always include `EXPLAIN ANALYZE` for performance checks.
- Prefer `JOIN` over subqueries where possible.
